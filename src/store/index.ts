import { useMenuStore } from '@/store/menu'
import { useThemeStore } from '@/store/theme'
import { useLocalStore } from '@/store/international'

export {
  useMenuStore,
  useThemeStore,
  useLocalStore
}
